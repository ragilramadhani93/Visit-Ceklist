import { User, Role, Checklist, Task, TaskPriority, ChecklistTemplate } from './types';

export const MOCK_USERS: User[] = [];


export const MOCK_CHECKLISTS: Checklist[] = [];

export const MOCK_TASKS: Task[] = [];


export const MOCK_TEMPLATES: ChecklistTemplate[] = [];
